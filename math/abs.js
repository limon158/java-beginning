// use of abs in calculation abs = abslute
const ageBride = 25;
const ageGroom = 34;
const gap = Math.abs(ageBride - ageGroom);
if(gap < 5)
{
    console.log('can be matched');
}
else
{
    console.log('cannot be match');
}
