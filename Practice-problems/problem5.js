// Isoscale or not
var side1= 9, side2=8, side3=9;
if(side1==side2 || side1==side3 || side2==side3){
    console.log("The triangle is isoscale");
} else {
    console.log("The triangle is not isoscale");
}